stages:
  - deploy

deploy_container:
  stage: deploy
  image: docker:23
  services:
    - docker:dind
  variables:
    DOCKER_BUILDKIT: 1
  script:
    # Anmelden in der Registry
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    # Buildx aktivieren
    - docker buildx create --use
    # Multi-Architektur-Image für ARM64 und AMD64 bauen und pushen
    - docker buildx build --platform linux/amd64,linux/arm64 -t $CI_REGISTRY_IMAGE:$CI_COMMIT_BRANCH --push .
  only:
    - main

# stages:
#   - deploy

# deploy_container:
#   stage: deploy
#   image: docker:23
#   services:
#     - docker:dind
#   script:
#     - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
#     - docker build --build-arg ARCH=arm64 -t $CI_REGISTRY_IMAGE:$CI_COMMIT_BRANCH .
#     - docker push $CI_REGISTRY_IMAGE:$CI_COMMIT_BRANCH
#   only:
#     - main
